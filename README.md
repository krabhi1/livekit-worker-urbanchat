# livekit-worker-urbanchat

### Commands
```bash
#python venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py  download-files
# run the worker
python main.py console|dev|start
```